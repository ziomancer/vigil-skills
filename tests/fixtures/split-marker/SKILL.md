---
name: split-marker
description: a case-2 tag that wraps onto the line after the tool token.
requires:
  shell: true
---

## Procedure

Look up the ticket (e.g., `mcp__plane__retrieve_work_item` in Claude Code,
or the equivalent in your host).
